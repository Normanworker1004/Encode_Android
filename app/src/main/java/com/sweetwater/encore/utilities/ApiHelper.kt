package com.sweetwater.encore.utilities

class ApiHelper {

    // A generic function that returns a Retrofit object based on the provided API interface.
    // The function takes a single type parameter, T, which is the API interface.
    // The function returns an object of type T, which is the API interface.
    // The function is called create().
    // The function create() returns an object of type T, which is the API interface.

}